[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
bevy = { workspace = true }
lightyear = { workspace = true }
rand = "0.10"
getrandom = {version = "0.4.2", features = ["wasm_js"]}  # align with rand
