[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true }
lightyear = { workspace = true, features = ["netcode", "webtransport"]}
common = { path = "../common" }
aeronet_webtransport = { workspace = true }